[project]
name = "p5"
version = "0.1.0"
description = "Flask with Bootstrap"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "flask>=3.1.3",
    "flask-cors>=6.0.2",
    "flask-limiter[cli]>=4.1.1",
    "routes>=2.5.1",
]
